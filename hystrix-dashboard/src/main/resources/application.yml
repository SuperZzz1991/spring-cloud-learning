server:
  port: 8501
spring:
  application:
    name: hystrix-dashboard

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8001/eureka/

#hystrix-dashboard允许代理白名单
hystrix:
  dashboard:
    proxy-stream-allow-list: '*'